import io, os, sys

# input
# 1. input = sys.stdin.readline
# 2. input = io.BytesIO(os.read(0, os.fstat(0).st_size)).readline
# ps : string need to input().decode() using method 2

# output
# sys.stdout.write("need to be string")
